// Drizzle-Schema. Noch leer — Tabellen hier definieren und dann
// `npm run db:generate` ausführen, um eine neue Migration zu erzeugen.
// Beim nächsten Container-Start wird die Migration automatisch angewendet.

export {};
